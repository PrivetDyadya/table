<h1>Add new item to the table of todo</h1>
<%= form_for @todo do |f| %>
	<%= f.label :name %> <%= f.text_field :name %>
	<%= f.hidden_field :done, value => false %>
	<%= f.submit "add to TODO list" %>
 



<% end %>